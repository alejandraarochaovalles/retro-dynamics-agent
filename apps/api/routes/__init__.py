# API contract endpoints (see packages/contracts/openapi.yaml):
#   sessions.py        — session lifecycle (create, join, start, close)
#   dynamics.py         — dynamic generation and library
#   liveblocks_auth.py   — authorization for access to Liveblocks rooms
#   consolidation.py     — groups and vote ranking after closing
#   action_items.py       — action item CRUD before export
#   export.py               — ticket creation in Jira/Azure DevOps
#   teams.py                  — teams and integrations
