# Vercel's Python builder (@vercel/python) actually installs from uv.lock,
# not this file (see docs/adr/0003, "Lesson learned" — this comment used to
# claim the opposite and that caused a production outage). Kept anyway,
# in sync with [project.dependencies] in pyproject.toml, since some tools/
# readers still expect a plain requirements.txt at this path.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
groq>=0.9
python-dotenv>=1.0
sqlalchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13
cryptography>=42
