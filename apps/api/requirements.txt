# Vercel's Python builder (@vercel/python) reads requirements.txt, not
# pyproject.toml — keep these in sync with the [project.dependencies] list
# in pyproject.toml (that file stays the source of truth for local dev via
# `pip install -e ".[dev]"` / CI).
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
groq>=0.9
python-dotenv>=1.0
sqlalchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13
